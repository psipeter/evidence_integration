node_modules/
dist/

# sequences.json in src/ is a copy of sequences/sequences.json — derived, not tracked
src/sequences.json

# pkl is binary generated by generate_sequences.py — json is the tracked version
sequences/sequences.pkl
