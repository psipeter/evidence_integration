node_modules/
dist/
dist-continuous/
dist-binary/
sequences/sequences.pkl

dev-results/
# Raw pilot participant data export (colors.txt/numbers.txt) -- generated/
# sensitive, same category as dev-results/ above, just not produced by the
# local dev server.
pilot1/
pilot2/
pilot3/
# Scratch scripts computing bonus payments directly from pilot3/'s raw
# data (hardcoded paths into pilot3/) -- same sensitivity category, not
# meant to be tracked.
compute_bonus_tmp.py
compute_bonus_numbers_tmp.py
# Throwaway 2-trial test jzips (manual JATOS testing only, built from a
# disposable test pool, never the real production sequences) -- distinct
# from the real tracked evidence-integration-{continuous,binary}.jzip.
*-TEST-2trial.jzip
task/.~lock.consent_form.doc#
